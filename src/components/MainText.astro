<div class="wrapper">
  <h1 class="title">Too the Moon!</h1>

  <p class="info">
    We’ll help you create, launch and grow your online store with Shopify.
  </p>

  <div class="button__wrapper">
    <button>Briefing</button>
    <button>Make Contact</button>
  </div>
</div>

<script>
  const buttonEls = [...document.querySelectorAll(".button__wrapper")];
  const event = {
    mouseEnter: new Event("mainTextButtonMouseEnter"),
    mouseLeave: new Event("mainTextButtonMouseLeave"),
  };

  buttonEls.forEach((el) => {
    el.addEventListener("mouseenter", () => {
      document.dispatchEvent(event.mouseEnter);
    });
    el.addEventListener("mouseleave", () =>
      document.dispatchEvent(event.mouseLeave)
    );
  });
</script>

<style>
  .wrapper {
    z-index: 1;
    position: absolute;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-end;
    gap: 1.5rem;
    padding-right: 25%;
    padding-bottom: 5rem;
    text-align: right;
    color: white;
    box-sizing: border-box;
    pointer-events: none;
  }

  .wrapper > * {
    pointer-events: all;
  }

  .title {
    margin: 0;
    font-size: 7rem;
    filter: url("#wiggly-effect");
    text-shadow: 0.5rem 0.5rem 0rem red;
  }

  .info {
    margin: 0;
    font-size: 1.5rem;
    filter: url("#wiggly-effect");
  }

  .button__wrapper {
    margin-top: 1.5rem;
    display: flex;
    gap: 1rem;
  }

  .button__wrapper > button {
    padding: 0.5rem;
    background-color: transparent;
    color: white;
    border: 0.35rem solid white;
    font-size: 2rem;
    font-weight: bold;
    letter-spacing: 0.15rem;
    cursor: pointer;
    filter: url("#wiggly-effect");
  }

  .button__wrapper > button:hover {
    filter: url("#wiggly-effect") drop-shadow(0.25rem 0.25rem 0rem red);
  }

  @media (max-width: 1700px) {
    .wrapper {
      padding-right: 10%;
    }
  }

  @media (max-width: 1250px) {
    .wrapper {
      padding-right: 5%;
      padding-bottom: 8rem;
    }

    .title {
      font-size: 5rem;
      text-shadow: 0.25rem 0.25rem 0rem red;
    }

    .info {
      font-size: 1.25rem;
    }

    .button__wrapper > button {
      border: 0.25rem solid white;
      font-size: 1.5rem;
    }
  }

  @media (max-width: 800px) {
    .wrapper {
      padding-right: 0;
      padding-bottom: 2rem;
      text-align: center;
      align-items: center;
    }

    .title {
      font-size: 5rem;
    }

    .info {
      font-size: 1.25rem;
    }
  }

  @media (max-width: 600px) {
    .title {
      font-size: 3rem;
    }

    .info {
      font-size: 1.25rem;
    }

    .button__wrapper {
      margin-top: 1rem;
    }

    .button__wrapper > button {
      border: 0.2rem solid white;
      font-size: 1.25rem;
    }
  }
</style>
